# =============================================================================
# Metadata Agent Canvas - Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# =============================================================================

# Server Configuration
# -----------------------------------------------------------------------------
PORT=3000

# CORS Configuration (comma-separated list)
# -----------------------------------------------------------------------------
ALLOWED_ORIGINS=http://localhost:3001,http://localhost:4200

# LLM Provider Configuration
# -----------------------------------------------------------------------------
# Options: 'openai', 'b-api-openai', 'b-api-academiccloud'
LLM_PROVIDER=b-api-openai

# OpenAI Configuration (required if LLM_PROVIDER=openai)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-proj-your-openai-api-key-here
OPENAI_MODEL=gpt-4.1-mini

# B-API Configuration (required if LLM_PROVIDER=b-api-openai or b-api-academiccloud)
# -----------------------------------------------------------------------------
B_API_KEY=your-b-api-key-here

# Rate Limiting Configuration
# -----------------------------------------------------------------------------
# Maximum LLM requests per minute per IP
RATE_LIMIT_LLM_MAX=150

# Maximum API requests per 15 minutes per IP
RATE_LIMIT_API_MAX=1500

# WLO Repository Guest Upload Configuration
# =============================================================================
# IMPORTANT: These credentials are used for guest uploads in:
#   - Local development mode
#   - Bookmarklet mode
#   - Container/Docker deployments
#   - Vercel/Netlify deployments
# 
# Keep these credentials secure and NEVER commit them to version control!
# =============================================================================

# ===========================
# Repository Settings
# ===========================

# WLO Guest Username (required)
WLO_GUEST_USERNAME=your-username

# WLO Guest Password (required)
# IMPORTANT: Use quotes if password contains # (to prevent dotenv treating it as comment)
WLO_GUEST_PASSWORD="your-user-password"

# WLO Repository Base URL
WLO_REPOSITORY_BASE_URL=https://repository.staging.openeduhub.net/edu-sharing

# =============================================================================
# Deployment-Specific Notes
# =============================================================================
#
# VERCEL:
# -------
# Set these as Environment Variables in your Vercel dashboard:
#   Project Settings > Environment Variables
#   - Mark sensitive variables (API keys, passwords) as "Encrypted"
#   - Set for Production, Preview, and Development environments as needed
#
# NETLIFY:
# --------
# Set these as Environment Variables in your Netlify dashboard:
#   Site Settings > Build & Deploy > Environment > Environment Variables
#   - Use "Sensitive variable" option for API keys and passwords
#   - They will be write-only and never readable again after saving
#
# DOCKER:
# -------
# Pass environment variables via docker-compose.yml or docker run:
#   docker run -e WLO_GUEST_USERNAME=xxx -e WLO_GUEST_PASSWORD=xxx ...
#
# =============================================================================
